services:
  - type: worker
    name: powerbang-bot
    env: node
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: TOKEN
        sync: false
      - key: NODE_ENV
        value: production
